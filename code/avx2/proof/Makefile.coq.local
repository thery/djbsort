# Build the shared library (code/common) before anything here, so that a plain
# `make` in this directory still works on its own.
pre-all::
	$(MAKE) -C ../../common

clean::
	$(MAKE) -C ../../common clean
